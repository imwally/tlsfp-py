# tlsfp-py

```
usage: tlsfp.py [-h] [-a A] hostname

Get TLS fingerprint from hostname.

positional arguments:
  hostname    (e.g., duckduckgo.com)

optional arguments:
  -h, --help  show this help message and exit
  -a A        SHA1 or SHA256 algorithm
  ```
